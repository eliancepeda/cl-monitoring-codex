# Set both live variables for live mode. Remove both for SQLite-only fallback mode.
CRAWLAB_BASE_URL=https://your-crawlab-host
CRAWLAB_TOKEN=replace_me

# Optional local runtime settings.
CL_MONITORING_DB_PATH=cl-monitoring.sqlite3
APP_PORT=8787
