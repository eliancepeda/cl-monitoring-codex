"""Repository — read/write access to local SQLite.

Note: this is local persistence, NOT Crawlab.
Write operations here are safe (writing to our own DB).
"""

# TODO: save_spiders(), get_spiders(), save_tasks(), get_tasks(), ...
